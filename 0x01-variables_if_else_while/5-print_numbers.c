#include <stdio.h>

/**
 * main - print all single digit number
 *
 * Return: 0 (success)
 */

int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		printf("%d", c);
	}
	printf("\n");
	return (0);
}
