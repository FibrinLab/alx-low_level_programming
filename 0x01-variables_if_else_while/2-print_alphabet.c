#include <stdio.h>

/**
 * main - Prints the alphabet in lower case
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		printf("%c\n", c);
	}
	return (0);
}
