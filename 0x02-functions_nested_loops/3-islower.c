#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * Return: 0 (success)
 */

int _islower(int c)
{
	int char;

	while(a < 'a')
	{
		if (char !== a)
		{
			return (1)
		}
		else
		{
			return (0)
		}
		a++;
	}	
	return (0);
}
