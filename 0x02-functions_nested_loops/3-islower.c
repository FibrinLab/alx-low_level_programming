#include "main.h"

/**
 * _islower - checks for lowercase character
 *
 * Return: 0 (success)
 */

int _islower(int c)
{
	int a;

	while(a < 'a')
	{
		if (a !== a)
		{
			return (1)
		}
		else
		{
			return (0)
		}
		a++;
	}	
	return (0);
}
