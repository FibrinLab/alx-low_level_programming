#include "main.h"

/**
 * _isalpha - checks if an alphabetic character
 *
 * Return: Always 0 (success)
 */

int _isalpha(int c)
{
	int c;

	for (c = 'A'; c <= 'z'; c++)
	{
		_putchar(c)
	}
	return (0);
}
